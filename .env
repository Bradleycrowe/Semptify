## Semptify environment example (copy to .env; never commit secrets)

# Security mode: open | enforced
SECURITY_MODE=open

# Legacy single admin token (fallback if multi-token file absent)
ADMIN_TOKEN=changeme_admin_token

# Flask session / CSRF secret (generate strong random hex)
FLASK_SECRET=replace_with_random_hex

# Rate limiting configuration
ADMIN_RATE_WINDOW=60
ADMIN_RATE_MAX=60
ADMIN_RATE_STATUS=429
ADMIN_RATE_RETRY_AFTER=60

# Logging rotation size (bytes)
LOG_MAX_BYTES=1048576

# GitHub integration (release tagging)
GITHUB_OWNER=Bradleycrowe
GITHUB_REPO=Semptify
# GITHUB_TOKEN=ghp_xxxxx   # supply via secrets manager

# Build metadata (usually injected by CI)
GIT_SHA=dev
BUILD_TIME=unknown

# Service port
SEMPTIFY_PORT=8080
