# 📅 Calendar Logic - Visual Flow Diagrams

Quick reference visual guides for how the calendar system works.

---

## 🔄 Main Calendar Flow

```
┌─────────────────────────────────────────────────────────────┐
│           CALENDAR EVENT LIFECYCLE                           │
└─────────────────────────────────────────────────────────────┘

CREATION:
┌──────────────┐
│ User Action  │  ← User fills form or API call
└──────┬───────┘
       │
       ↓
┌──────────────────────────┐
│ CalendarEvent Object     │  ← Create event with:
│ - title                  │    • title
│ - event_date             │    • date/time
│ - event_type             │    • type
│ - priority               │    • priority
│ - description            │    • description
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Add to Calendar:         │  ← Thread-safe lock
│ - Save to _events list   │  ← Append to calendar.json
│ - Thread lock acquired   │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Event stored and indexed │  ← Ready for queries
└──────────────────────────┘

RETRIEVAL:
┌──────────────────────┐
│ User Query           │  ← GET /api/ledger-calendar/calendar
│ (with filters)       │    with optional params
└──────┬───────────────┘
       │
       ↓
┌──────────────────────────────┐
│ Calendar.get_events()        │  ← Load _events from memory
│ - Filter by date range       │
│ - Filter by type             │
│ - Filter by priority         │
│ - Filter by completed status │
└──────┬───────────────────────┘
       │
       ↓
┌──────────────────────────────┐
│ Sort results:                │  ← Sort by event_date
│ - By date ascending          │
│ - By priority (if requested) │
└──────┬───────────────────────┘
       │
       ↓
┌──────────────────────────────┐
│ Return filtered/sorted list  │  ← JSON response to user
└──────────────────────────────┘

COMPLETION:
┌──────────────┐
│ Mark Done    │  ← User clicks "Complete"
└──────┬───────┘
       │
       ↓
┌──────────────────────────┐
│ Mark_completed()         │  ← Set completed=true
│ - Set completed flag     │  ← Set completed_at time
│ - Update completed_at    │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Re-write calendar file   │  ← Update persistent storage
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Event marked complete    │  ← No longer in "upcoming"
└──────────────────────────┘
```

---

## 🎯 Event Type Decision Tree

```
┌─────────────────────────────┐
│   Creating New Event?       │
└─────────────┬───────────────┘
              │
              ↓
    ┌─────────────────────┐
    │ What is the event?  │
    └────────┬────────────┘
             │
    ┌────────┼────────┬───────────┬─────────────┐
    │        │        │           │             │
    ↓        ↓        ↓           ↓             ↓
┌──────┐ ┌──────┐ ┌────────┐ ┌──────────┐ ┌──────────┐
│Hard  │ │Soft  │ │Task to │ │Something │ │Task is   │
│date? │ │date? │ │do?     │ │done?     │ │past due? │
└──┬───┘ └──┬───┘ └───┬────┘ └────┬─────┘ └────┬─────┘
   │        │        │            │            │
   │yes     │yes     │yes         │yes        │maybe
   │        │        │            │           │
   ↓        ↓        ↓            ↓           ↓
┌──────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐
│      │ │REMINDER: │ │ ACTION   │ │COMPLETED:│ │Check:  │
│      │ │          │ │NEEDED:   │ │          │ │now >   │
│      │ │Soft date │ │          │ │Historical│ │date?   │
│      │ │Informl.  │ │Do by     │ │Already   │ └───┬────┘
│      │ │reminder  │ │this date │ │done      │     │
│      │ │          │ │          │ │          │  ┌──┴──┐
│      │ │Example:  │ │Example:  │ │Example:  │  │No   │
│      │ │"Review   │ │"Send     │ │"Notice   │  │     │
│      │ │ lease"   │ │ notice"  │ │ sent"    │  │Act as
│      │ │          │ │          │ │          │  │DEADLINE
└──────┘ └──────────┘ └──────────┘ └──────────┘  └──────┘
   │        │        │            │
   │        │        │            │
   ↓        ↓        ↓            ↓
type:    type:      type:       type:
deadline reminder   action_     completed
         needed
```

---

## 📊 Filtering Logic Flow

```
┌────────────────────────────────────────────┐
│ User requests: GET /calendar              │
│ ?start_date=2025-11-01                    │
│ &end_date=2025-11-30                      │
│ &type=deadline                            │
│ &priority=2                               │
│ &completed=false                          │
└────────┬─────────────────────────────────┘
         │
         ↓
    ┌─────────────────┐
    │ Start with      │  (all events: 50 total)
    │ all events      │
    └────────┬────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Filter: start_date >= 2025-11-01
    │ Remove events before Nov 1   │  (now: 40 events)
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Filter: end_date <= 2025-11-30
    │ Remove events after Nov 30   │  (now: 35 events)
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Filter: type == "deadline"   │
    │ Keep only deadlines          │  (now: 20 events)
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Filter: priority == 2        │
    │ Keep only high priority      │  (now: 5 events)
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Filter: completed == false   │
    │ Keep only incomplete         │  (now: 4 events)
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Sort by: event_date (asc)    │
    │ Order from earliest to latest│
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │ Return 4 events (JSON)       │
    │ Ready for display            │
    └──────────────────────────────┘
```

---

## ⏰ Upcoming Events Logic

```
REQUEST: Get events in next 7 days

Current Time: 2025-11-04 14:30:00
┌────────────────────────────────┐
│ Now                            │
│ 2025-11-04 14:30:00           │
└────┬───────────────────────────┘
     │
     ├─────────────────────────────────── Events Window ─────────────────
     │                                                                   │
     ↓                                                                   ↓
2025-11-04                                                      2025-11-11
TODAY                                                           TODAY + 7 DAYS

Events in window:
┌─────────────────────────────────────────────────────────────────┐
│ Event 1: 2025-11-05 09:00 - Priority 0 (Low)                  │
│ Event 2: 2025-11-06 10:00 - Priority 2 (High) ← SORT FIRST   │
│ Event 3: 2025-11-08 14:30 - Priority 1 (Medium)              │
│ Event 4: 2025-11-10 11:00 - Priority 2 (High) ← SORT FIRST   │
│ Event 5: 2025-11-12 09:00 - Outside window (EXCLUDE)          │
└─────────────────────────────────────────────────────────────────┘

SORT ALGORITHM:
1. Primary sort: Priority (descending: 2→1→0)
2. Secondary sort: Date (ascending: early→late)

RESULT (sorted for user):
[
  Event 2: 2025-11-06 (Priority 2) ← Do this first!
  Event 4: 2025-11-10 (Priority 2) ← Then this
  Event 3: 2025-11-08 (Priority 1) ← Then this
  Event 1: 2025-11-05 (Priority 0) ← Then this
]

Display: Show high priorities first, regardless of date!
```

---

## 🔗 Calendar ↔ Ledger Integration

```
SCENARIO: Track a multi-step process

┌─────────────────────────────────────┐
│ STEP 1: User takes action           │
└─────────┬───────────────────────────┘
          │
          ↓
     ┌─────────────┐
     │ Add Ledger  │  Ledger Entry:
     │ Entry       │  - Type: "notice"
     └─────┬───────┘  - Description: "Sent notice"
           │          - Timestamp: 2025-11-04 10:00:00
           │          - Reference: "Certified #123"
           │
           └─────────┬────────────────────────────────────┐
                     │                                    │
                     ↓                                    ↓
            ┌──────────────────┐          ┌──────────────────────┐
            │ Get Entry ID     │          │ (Also add)           │
            │ ledger-uuid-001  │          │ Calendar Event       │
            └────┬─────────────┘          │ - Title: "Follow-up" │
                 │                         │ - Date: 2025-11-11   │
                 │                         │ - related_entry_id:  │
                 │                         │   ledger-uuid-001 ←─┐
                 │                         └──────────────────────┘
                 │                              ↑
                 └──────────────────────────────┘
                 (Link them together!)

RESULT:
┌────────────────────────────────────┐
│ LEDGER SHOWS                       │
├────────────────────────────────────┤
│ When: 2025-11-04 10:00:00         │
│ What: Sent notice                 │
│ Ref: Certified #123               │
│ Related Event: follow-up           │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ CALENDAR SHOWS                     │
├────────────────────────────────────┤
│ When: 2025-11-11                  │
│ What: Follow-up                   │
│ Related Action: ledger-uuid-001   │
│ Links to original: "Sent notice"  │
└────────────────────────────────────┘

When user clicks event → Shows original action from ledger!
```

---

## 📈 Priority Color Coding

```
PRIORITY LEVELS (displayed on calendar)

Priority 0: LOW
┌──────────────────┐
│ ⬜ Gray (#6c757d) │
│ Non-urgent items │
│ Can wait         │
│ Example:         │
│ "Check code..."  │
└──────────────────┘

Priority 1: MEDIUM
┌──────────────────┐
│ 🟡 Yellow (#fc1107)
│ Important        │
│ Do this month    │
│ Example:         │
│ "Schedule insp.."│
└──────────────────┘

Priority 2: HIGH
┌──────────────────┐
│ 🔴 Red (#d13438) │
│ Urgent/Critical  │
│ Do immediately   │
│ Example:         │
│ "Court deadline" │
└──────────────────┘

Visual on Calendar:
Month view shows dots with colors:
⬜⬜🟡⬜🔴⬜🟡⬜⬜🔴🟡⬜
```

---

## 💾 Data Persistence Flow

```
IN-MEMORY:
┌──────────────────────┐
│ Calendar._events     │
│ [                    │
│   CalendarEvent(...),
│   CalendarEvent(...),
│   CalendarEvent(...)
│ ]                    │
└──────────────────────┘
(Fast access, lost on restart)

         ↓ on add_event()

FILE PERSISTENCE:
┌──────────────────────┐
│ data/calendar.json   │
│ (append-only)        │
│                      │
│ Line 1: {"id"...     │
│ Line 2: {"id"...     │
│ Line 3: {"id"...     │
└──────────────────────┘
(Durable, persists after restart)

ON APPLICATION START:
1. Clear memory: _events = []
2. Open calendar.json
3. Read line by line
4. Parse JSON
5. Create CalendarEvent objects
6. Add to _events list
7. Memory now matches file!
```

---

## 🧵 Thread Safety

```
WITHOUT LOCK:
┌──────────────┐           ┌──────────────┐
│ Thread A     │           │ Thread B     │
│ add_event()  │           │ add_event()  │
└──────┬───────┘           └──────┬───────┘
       │                          │
       ↓ (Race!)                  ↓
    Read file ──────────┬────── Read file
    Add event A         │      Add event B
    Write file          │      Write file
    (Event B lost!) ◄───┘      (Oops!)
        
RESULT: Corrupted data, missing events

WITH LOCK:
┌──────────────┐           ┌──────────────┐
│ Thread A     │           │ Thread B     │
│ add_event()  │           │ add_event()  │
└──────┬───────┘           └──────┬───────┘
       │                          │
       ↓                          ↓
   [WAITING FOR LOCK]         [WAITING FOR LOCK]
       │                          │
   ┌───┴──────────────────────────┴───┐
   │ Thread A acquires lock            │
   │ Adds event safely                 │
   │ Releases lock                     │
   └───┬─────────────────────────┬─────┘
       │                         │
       └─ Thread B acquires lock ─┘
           Adds event safely
           Releases lock
       
RESULT: All events persisted correctly!
```

---

## 📋 Query Patterns

```
Pattern 1: GET ALL UPCOMING
GET /api/ledger-calendar/calendar?upcoming_days=7

Pattern 2: GET FILTERED BY TYPE
GET /api/ledger-calendar/calendar?type=deadline

Pattern 3: GET BY DATE RANGE
GET /api/ledger-calendar/calendar?start_date=2025-11-01&end_date=2025-11-30

Pattern 4: GET HIGH PRIORITY INCOMPLETE
GET /api/ledger-calendar/calendar?priority=2&completed=false

Pattern 5: GET ALL FOR EXPORT
GET /api/ledger-calendar/calendar

Pattern 6: GET OVERDUE
GET /api/ledger-calendar/calendar?end_date=<today>&completed=false
```

---

## ✅ Summary

**Calendar System Architecture:**

1. **Storage:** Append-only JSON file + in-memory list
2. **Concurrency:** Thread locks for safety
3. **Query:** Flexible filtering by date, type, priority
4. **Integration:** Links to ledger via related_entry_id
5. **Lifecycle:** Create → Query → Update → Complete
6. **Display:** Color-coded by priority, sorted intelligently

**Key Formula:**
```
Event = Title + DateTime + Type + Priority + Status
Query = Filter(events) by: date range, type, priority, complete
Display = Sort by: priority first, then date
```

