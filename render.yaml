services:
  - type: web
    name: semptify
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_prod.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: GROQ_API_KEY
        sync: false
