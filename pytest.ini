[pytest]
# Only search for tests under the top-level `tests/` directory and
# ignore common runtime/backup directories that contain duplicate test
# module names which can confuse pytest's import machinery.
testpaths = tests
norecursedirs = backups .venv* .venv .git .github SemptifyGUI
