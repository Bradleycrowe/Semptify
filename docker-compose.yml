version: "3.8"
services:
  semptify:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SEMPTIFY_PORT=8080
    volumes:
      - ./uploads:/app/uploads
      - ./final_notices:/app/final_notices
      - ./logs:/app/logs
    restart: unless-stopped
